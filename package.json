{
  "name": "proyek-akhir",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "babel-plugin-react-compiler": "^19.1.0-rc.2",
    "concurrently": "^9.1.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "start-all": "concurrently -k -s cmd \"bun run dev --prefix ./frontend\" \"bun start --prefix ./backends/*\"",
    "dev-all": "concurrently -k -s cmd \"bun run dev --prefix ./frontend\" \"bun dev --prefix ./backends/*\""
  },
  "type": "module",
  "dependencies": {
    "express": "^5.1.0"
  }
}